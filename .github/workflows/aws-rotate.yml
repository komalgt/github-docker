name: Rotate AWS Credentials

on:
  workflow_dispatch: 
  schedule:
    - cron: '0 3 1 * *' # Monthly at 3am UTC

jobs:
  rotate-aws-credentials:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: 3.11

      - name: Install dependencies
        run: |
          pip install boto3 requests pynacl

      # <<< Inserted step to display the active AWS Access Key ID
      - name: Show active AWS Access Key ID
        run: echo "Access Key ID is $AWS_ACCESS_KEY_ID"
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      # >>>

      - name: Rotate AWS keys and update GitHub secrets
        env:
